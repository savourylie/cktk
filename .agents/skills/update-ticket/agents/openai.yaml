interface:
  display_name: "Update Ticket"
  short_description: "Verify ticket status and commit scoped tracker updates"
  default_prompt: "Use $update-ticket to reconcile the selected ticket and its tracker in the correct checkout, mark Done directly when evidence is sufficient, and commit only the intended document changes."

policy:
  allow_implicit_invocation: false
