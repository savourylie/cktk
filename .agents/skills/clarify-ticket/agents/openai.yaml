interface:
  display_name: "Clarify Ticket"
  short_description: "Clarify a markdown ticket against the codebase"
  default_prompt: "Use $clarify-ticket to read one docs/tickets/ ticket, validate it against tracker and code context, discuss ambiguities and risks, and produce a read-only readiness summary."

policy:
  allow_implicit_invocation: false
