interface:
  display_name: "Update Ticket (Linear)"
  short_description: "Update Linear from evidence without redundant confirmation"
  default_prompt: "Use $update-ticket-linear to reconcile the issue and affected dependencies, mark Done directly when evidence is sufficient, and handle independently authorized document follow-up without git commits."

policy:
  allow_implicit_invocation: false
