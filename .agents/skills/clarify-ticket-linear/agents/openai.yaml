interface:
  display_name: "Clarify Ticket (Linear)"
  short_description: "Clarify a Linear issue against relevant code"
  default_prompt: "Use $clarify-ticket-linear to fetch one Linear issue with read-only tools, analyze it against relevant code when available, discuss ambiguities and risks, and produce a readiness summary."

policy:
  allow_implicit_invocation: false
