interface:
  display_name: "Implement Ticket (Linear)"
  short_description: "Implement a Linear issue with validation and review (no commit, no Linear writes)"
  default_prompt: "Fetch the named Linear issue via MCP, implement it, validate, and review. Do not commit and do not update Linear."

policy:
  allow_implicit_invocation: false
