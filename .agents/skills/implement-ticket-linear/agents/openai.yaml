interface:
  display_name: "Implement Ticket (Linear)"
  short_description: "Implement Linear issues with business context and review"
  default_prompt: "Use $implement-ticket-linear to understand the issue's business role and related tickets, clarify conflicting definitions, then implement and verify it, including from Backlog and with worktree isolation when requested."

policy:
  allow_implicit_invocation: false
