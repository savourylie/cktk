interface:
  display_name: "Commit Push PR"
  short_description: "Create one commit, push the branch, and open a pull request"
  default_prompt: "Inspect the current git state, commit the intended changes, push the branch, and open a pull request."

policy:
  allow_implicit_invocation: false

