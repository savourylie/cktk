interface:
  display_name: "Debrief Result"
  short_description: "Explain a result's meaning for the project"
  default_prompt: "Use $debrief-result to explain what the just-delivered result means for the project: what the product can now do, what this unlocks, and what is still missing."

policy:
  allow_implicit_invocation: true
