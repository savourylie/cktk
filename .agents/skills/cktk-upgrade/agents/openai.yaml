interface:
  display_name: "cktk Upgrade"
  short_description: "Upgrade cktk skills and reconcile local agent installs"
  default_prompt: "Detect the active cktk install, pull the latest version from GitHub, run the all-agent installer to reconcile Claude Code, Codex, Antigravity, OpenCode, and handoff helper installs, then report what changed."

policy:
  allow_implicit_invocation: false
