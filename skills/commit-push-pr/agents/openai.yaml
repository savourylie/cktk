interface:
  display_name: "Commit Push PR"
  short_description: "Commit as needed, push, and create or reuse a GitHub PR"
  default_prompt: "Use $commit-push-pr to commit the intended pending work if needed, push the selected branch, and create or reuse a PR with the correct head/base and an explanation of the complete change."

policy:
  allow_implicit_invocation: false
