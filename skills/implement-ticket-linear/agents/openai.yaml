interface:
  display_name: "Implement Ticket (Linear)"
  short_description: "Implement Linear issues and explain outcomes or blockers"
  default_prompt: "Use $implement-ticket-linear to clarify the issue's business context, implement and verify it in the requested checkout, then clearly report completion and PR/merge readiness or blockers and next steps, connecting business value to technical progress."

policy:
  allow_implicit_invocation: false
